{
  "name": "wavecrest-pro-root",
  "private": true,
  "scripts": {
    "install": "cd server && npm install",
    "start": "cd server && node index.js",
    "migrate": "cd server && node scripts/migrate.js",
    "seed": "cd server && node scripts/seed.js"
  }
}
