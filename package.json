{
  "name": "wavecrest-pro",
  "private": true,
  "version": "2.0.0",
  "description": "Wavecrest Pro — Creator intelligence. Decode any creator's playbook.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "migrate": "node scripts/migrate.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "pg": "^8.13.3"
  }
}
