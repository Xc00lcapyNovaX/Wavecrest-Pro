name: Update Trends

on:
  schedule:
    # Runs daily at 6:00 AM UTC
    - cron: '0 6 * * *'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  update-trends:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run trend updater
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: python3 update-trends.py

      - name: Commit and push if changed
        run: |
          git config user.name "Xc00lcapyNovaX"
          git config user.email "efeali.bel@gmail.com"
          git add public/trends.json
          git diff --cached --quiet || git commit -m "Update daily trends $(date -u +%Y-%m-%d)"
          git push

      - name: Ping server to seed new trends into DB
        run: |
          curl -s -X POST https://wavecrest.pro/api/trends/seed \
            -H "x-seed-secret: ${{ secrets.SEED_SECRET }}" \
            -H "Content-Type: application/json" \
            --max-time 30 \
            --retry 3 \
            --retry-delay 5 \
            || echo "Seed ping failed (non-fatal)"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install server dependencies
        run: npm install --prefix server

      - name: Send daily digest emails
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          BASE_URL: https://wavecrest.pro
          NODE_ENV: production
        run: node server/scripts/send-digest.js || echo "Digest send failed (non-fatal)"
